import { RecipesDirective } from './recipes.directive';

describe('RecipesDirective', () => {
  it('should create an instance', () => {
    const directive = new RecipesDirective();
    expect(directive).toBeTruthy();
  });
});
